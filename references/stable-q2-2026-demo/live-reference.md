# Live Reference

This file tracks how to inspect the stable Q2 2026 demo as reference material.

## Live URL

```text
TBD
```

Use the live demo as the primary source for behavior, motion, responsive quality, and presentation feel when it is available.

## Local Archived Source Path

```text
references/stable-q2-2026-demo/archived-source/
```

The archived source is available for audit only. It must not be imported into the new platform runtime.

## Local Preview

Option 1: open the archived `index.html` directly in a browser:

```text
references/stable-q2-2026-demo/archived-source/index.html
```

Option 2: serve the archived source with a simple local static server:

```bash
cd references/stable-q2-2026-demo/archived-source
python3 -m http.server 8000
```

Then open:

```text
http://localhost:8000
```

## Audit-Only Rule

The live demo and archived source are used only for reference audit:

- brand DNA;
- premium visual atmosphere;
- mobile lessons;
- motion lessons;
- asset inventory;
- risks and opportunities for the new platform.

Do not copy the old fixed 5-slide structure, old navigation, loader, layout, CSS classes, JavaScript logic, or plain HTML/CSS/JS stack into the new platform as architecture.
